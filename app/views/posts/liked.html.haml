%h2
  %span.label.label-primary= "Your liked posts:"
.row
  .posts
    = render @posts
%br

- if @posts.next_page
  .row.text-center
    .pagination
      = link_to "Load more", liked_posts_path(page: @posts.next_page), remote: true, class: "btn btn-lg btn-primary"
= render "shared/post_modal"
