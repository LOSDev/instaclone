%h3 Your feed:

.row
  .posts
    = render @posts
%br

- if @posts.next_page
  .row.text-center
    .pagination
      = link_to "Load more", feed_user_path(current_user, page: @posts.next_page), remote: true, class: "btn btn-lg btn-primary"
.modal.fade{tabindex: "-1", role: "dialog"}
